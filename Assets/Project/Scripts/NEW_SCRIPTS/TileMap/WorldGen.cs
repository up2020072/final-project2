using UnityEngine;

public class WorldGen : MonoBehaviour
{
    //start with variables and empty functions

    //file save path
    //@"D:\" + "level" + level + "\\ " + cx + "_" + cy + ".json", json
}
